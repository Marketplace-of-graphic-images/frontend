import {
  openModalAuth,
  closeModal,
  openModalRegister,
  openModalPassRecovery,
} from './modalSlice';

import { setUser, clearUser } from './userSlice';

export {
  openModalAuth,
  closeModal,
  openModalRegister,
  openModalPassRecovery,
  setUser,
  clearUser,
};

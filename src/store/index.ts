import {
  openModalAuth,
  closeModal,
  openModalRegister,
} from './systemSlice';

import { setUser, clearUser } from './userSlice';

export {
  openModalAuth,
  closeModal,
  openModalRegister,
  setUser,
  clearUser,
};
